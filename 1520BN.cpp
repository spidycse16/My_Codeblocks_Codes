#include<bits/stdc++.h>
using namespace std;
int main()
{
    if(c<=people/2)
        x=x+(people/2);
    else
        x=x-(people/2);
    if(x<=people)
        cout<<x<<endl;
    else
        cout<<"-1"<<endl;
}

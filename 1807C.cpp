#include<bits/stdc++.h>
using namespace std;
vector <long long int> v;
vector <long long int> s;
int main()
{
    int tc,i,j,x,y,k;
    string s;
    cin>>tc;
    for(i=0 ; i<tc ; i++)
    {
        cin>>s;
        char ch;
        for(j=0 ; j<s.size()-1 ; j++)
        {
            for(k=j+1 ; k<size() ; k++)
            {
                if(s[k]==s[j])

            }
        }
    }

}


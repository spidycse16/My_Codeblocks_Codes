#include<bits/stdc++.h>
using namespace std;
int main()
{
    string a,b;
    vector <int> x;
    vector <int> y;
    int,base,len,z,m,n;
    cin>>base;
    cin>>a>>b;
    for(int i=0 ; i<a.size() ; i++)
    {
        z=a[i]-'0';
        x.push_back(z);
    }
     for(int i=0 ; i<b.size() ; i++)
    {
        z=b[i]-'0';
        y.push_back(z);
    }
    for(int i=1 ; i<=a.size() ; i++)
    {

    }
}

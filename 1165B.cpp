#include<bits/stdc++.h>
using namespace std;
int main()
{
    int tc,i,j,ans;
    vector <int> v;
    int cnt=0;
    cin>>tc;
    for(i=0;i<tc;i++)
    {
        int y;
        cin>>y;
        v.push_back(y);
    }
    sort(v.begin(),v.end());
    while(cnt!=tc)
    {
        for(i=0 ; i<v.size() ; i++)
        {
            if()
        }
    }
    cout<<i<<endl;
}

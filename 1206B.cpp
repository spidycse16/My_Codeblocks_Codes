#include<bits/stdc++.h>
using namespace std;
int main()
{
    int tc,x,y,n,i,n,ans=0,cnt=0;
    cin>>n;
    vector <int> v;
    for(i=0 ; i<n ; i+++)
    {
        cin>>x;
        v.push_back(x);
        if(x<0)
            cnt++;
    }
    sort(v.begin(),v.end());
   for(i=0 ; i<v.size() ; i++)
   {
       if(v[i])
   }


}
